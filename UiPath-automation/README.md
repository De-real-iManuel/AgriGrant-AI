UiPath Automation Projects
=========================

This folder contains UiPath automation projects for AgriGrant AI.

- Generated files (project artifacts, `.entities`, `.project`, `.app`, `.git` folders, and archives) are intentionally ignored via the repository `.gitignore`.
- If you need to include a generated file, add it explicitly and remove the matching ignore pattern.

Purpose: keep the `UiPath-automation` directory present in the repo and document expected files.
